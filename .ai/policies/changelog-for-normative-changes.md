# Changelog for normative and user-visible changes

**ACS type:** Policy

1. Any edit to `spec/ACS-v0.1.md` **Sections 1–10** that changes compliance meaning **MUST** include a `CHANGELOG.md` entry in the same change set (version section per repository practice).
2. **SHOULD** log substantive publisher-doc or template/example drift fixes in `CHANGELOG.md` when adopters would notice.
3. **MUST NOT** retroactively rewrite published `CHANGELOG.md` history in a way that conceals prior normative text; append corrections instead.
