# Discoverability from AGENTS.md

**ACS type:** Policy

1. Every contract under `.ai/` **MUST** be reachable by following links starting from root `AGENTS.md` (typically via `.ai/index.md` and then per-category lists).
2. **MUST NOT** hide maintainer-only rules exclusively in `CONTRIBUTING.md` or GitHub wikis if they are required for safe editing of the normative spec—mirror or summarize them under `.ai/instructions/` or `.ai/policies/` when they affect agent behavior on this repo.
3. New `.ai/` files **MUST** be added to `.ai/index.md` in the same change set.
