# Specification integrity

**ACS type:** Policy

- Do not silently contradict §1 normative text in templates, examples, or `README.md` without updating the spec or clearly marking content as illustrative.
- If you change the required directory layout or skill packaging rules in §1, update every path in `templates/`, `examples/`, and this repository’s root `AGENTS.md` / `.ai/` in the same change set when practical.
