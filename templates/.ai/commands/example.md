# Command (example)

**ACS type:** Command  
**Location:** `.ai/commands/*.md`  
**Scope:** Invoked workflow — a named process (for example “release checklist” or “onboard contributor”).

Document the trigger (how the user or system invokes it), ordered steps, and expected outputs or artifacts.
