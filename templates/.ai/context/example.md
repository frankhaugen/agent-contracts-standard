# Context (example)

**ACS type:** Context  
**Location:** `.ai/context/*`  
**Scope:** Read-only structured data for agents (maps, inventories, generated summaries).

This example uses Markdown. You may use other formats (for example JSON such as [`mcp-tool-contract.json`](mcp-tool-contract.json)) if they remain machine- or human-readable and are treated as read-only reference unless a higher-level contract says otherwise.
