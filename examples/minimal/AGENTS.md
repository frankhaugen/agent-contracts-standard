# Minimal example repository

Tiny project used only to demonstrate the smallest ACS layout from `spec/ACS-v0.1.md` §2.3.

## Project overview

No application code in this folder: it is documentation-only layout for copy-paste reference.

## Build and test

None. This example has no build or automated tests.

## Security considerations

Treat any future secrets in forks of this pattern like production credentials: never commit them.

## Agent contract (ACS)

The full contract lives under `.ai/`. Open `.ai/index.md` first.

## Required reads

1. `.ai/index.md`
2. `.ai/instructions/`
3. `.ai/policies/`

## Rules

- Follow instructions before making changes
- Enforce policies at all times
- Use skills when applicable
- Use commands for structured workflows
- Do not introduce new conventions if defined in `.ai/`
