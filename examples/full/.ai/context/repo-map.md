# Repository map (context)

**ACS type:** Context (read-only reference)

| Path        | Role                          |
|------------|--------------------------------|
| `src/`     | Application source            |
| `tests/`   | Automated tests               |
| `docs/`    | Human-oriented documentation  |
| `.ai/`     | Agent contracts (ACS)         |
| `.ai/skills/<skill-id>/` | [Agent Skills](https://agentskills.io/) packages (`SKILL.md`) |

Update this file when top-level layout changes so agents have a stable orientation artifact.
